<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Design.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="my_btn" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorBlue</item>
        <item name="colorButtonNormal">#42a5f5</item>
    </style>
    <style name="my_btn_exit" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorRed</item>
        <item name="colorButtonNormal">#42a5f5</item>
    </style>
    <style name="my_btn_back" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/colorBlue</item>
        <item name="colorButtonNormal">#00FFFFFF</item>
    </style>
</resources>
